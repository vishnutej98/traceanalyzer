pipeline {
    agent { 
        node {
            label 'docker-agent-python'
            }
      }
    triggers {
        pollSCM '* * * * *'
    }
    stages {
        stage('Build') {
            steps {
                echo "Building.."
                sh'''
                cd myapp
                pip install -r requirements.txt
                cd ..
                '''
            }
        }
        stage('Test') {
            steps {
                echo "Testing.."
                sh '''
                cd myapp
                python3 app.py
                python3 app.py --name=Vishnu --age=25
                python3 app.py --name=Archana --age=23
                python3 app.py --name=Kamal --age=21
                python3 app.py --name=Ravi --age=20
                python3 app.py --name=Bhuvan --age=18
                python3 app.py --name=Kriyesh --age=16
                '''
            }
        }
        stage('Deliver') {
            steps {
                echo 'Deliver....'
                sh '''
                echo "doing delivery stuff......"
                '''
            }
        }
    }
}